# no need to scan for other lombok config in parent directories
config.stopBubbling = true
# mark all generated methods with the @lombok.Generated
lombok.addLombokGeneratedAnnotation = true
#custom logger name
lombok.log.fieldName = logger
